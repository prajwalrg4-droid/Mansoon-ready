{
  "name": "monsoon-ready",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@google/genai": "^2.11.0"
  },
  "scripts": {
    "build": "node --check api/generate.js && node --check app.js"
  }
}
